interface AkunType {
  kode_akun_header: string;
  nama_akun_header: string;
  saldo_normal: string;
  company: string;
  kode_akun: string;
  nama_akun: string;
}

interface AkunHeaderType {
  kode_akun_header: string;
  nama_akun_header: string;
}

interface AkunDetailType {
  kode_akun_header: string;
  kode_akun: string;
  nama_akun: string;
}
